version: '3.8'
services:
  sqldata:
    image: mcr.microsoft.com/azure-sql-edge:latest
    container_name: shop_sql
    environment:
      - ACCEPT_EULA=Y
      - SA_PASSWORD=ShopPassword123!
    ports:
      - "1433:1433"
    volumes:
      - sql_data:/var/opt/mssql

volumes:
  sql_data:
